{
  "name": "storyplex",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered visual novel game using Monogatari + Gemini",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "cd server && python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3001",
    "dev:client": "npm run dev --workspace=client",
    "install:server": "cd server && python3 -m pip install -r requirements.txt",
    "build": "npm run build --workspace=client"
  }
}
